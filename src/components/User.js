import { useState } from "react";

const User = ({ name }) => {
  const [count] = useState(0);

  async function getUserInfo

  return (
    <div className="user-card">
      <h1>Count: {count}</h1>
      <h2>Name: {name}</h2>
      <h2>Location: Bengaluru</h2>
      <h4>Contact: {@hsingla378}</h4>
    </div>
  );
};

export default User;
